type VoteRecord implements Base {
  id: ID!
  userId: String!
  roundId: String!
  voteRoleId: String!
  voteType: String!
  isExtra: String!
  ip:String
  voteRole: VoteRole
  createdAt: Date
  updatedAt: Date
  status: String
  remark: String
  createBy: String
  updateBy: String
}
